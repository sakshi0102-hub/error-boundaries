import React from 'react';

function MyComponent() {
  return (
    <div>
      <h1>Error boundary task</h1>
      <p> Error boundaries are React components that implement either componentDidCatch lifecycle method or the static getDerivedStateFromError method. These methods are used to catch errors that occur during rendering, in lifecycle methods, and in constructors of the whole tree below them.</p>
    </div>
  );
}

export default MyComponent;
